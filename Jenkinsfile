pipeline {
     agent any
     stages {
        stage('Build') {
            steps {
                // Define the build steps here
                echo 'Building...'
            }
        }
        stage('Test') {
            steps {
                // Define the test steps here
                echo 'Testing...'
            }
        }
        stage('Deploy') {
            steps {
                // Define the deploy steps here
                echo 'Deploying...new code'
            }
        }
    }
}
